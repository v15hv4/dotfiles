/**
 * @file [:VIM_EVAL:]expand("%:t")[:END_EVAL:]
 * @brief <[:VIM_EVAL:]expand("%:t:r")[:END_EVAL:]>
 *
 * @author Vishva Saravanan R (v15hv4)
 */

#include <bits/stdc++.h>
using namespace std;

// custom definitions {{{
#define llint long long int

#define parr(arr, len) \
    for (int i = 0; i < len; i++) cerr << arr[i] << " ";
// }}}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}
